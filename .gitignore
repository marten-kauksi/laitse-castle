# Dependencies
node_modules/

# Build output
dist/

# Astro
.astro/

# Environment
.env
.env.*
!.env.example
# Use docs/posthog-env.example as the template (hooks may block writing .env.example)

# OS
.DS_Store

# Research & scraped data (not part of site)
_research/
scraped/
.playwright-cli/
qa-screenshots/

# Master photo originals (served as WebP from public/fotopank/, not deployed)
# Leading slash anchors to repo root so it does NOT also match public/fotopank/
# (the filesystem is case-insensitive, so an unanchored "Fotopank/" caught both)
/Fotopank/