---
const currentPath = Astro.url.pathname;

const navLinks = [
  { label: 'Avaleht', href: '/' },
  { label: 'Ajalugu', href: '/ajalugu' },
  { label: 'Peod', href: '/peod' },
  { label: 'Seminarid', href: '/seminarid' },
];
---

<header class="sticky top-0 z-50 bg-warm-white border-b border-black/5">
  <nav class="pl-0 pr-4 py-1 md:pr-6 md:py-1.5 flex items-center justify-between max-w-7xl mx-auto">
    <a href="/" class="shrink-0">
      <img src="/images/logo-200.png" alt="Laitse Loss" class="h-[40px] md:h-[56px] w-auto" />
    </a>

    <!-- Desktop nav -->
    <ul class="hidden md:flex items-center gap-8">
      {navLinks.map(({ label, href }) => (
        <li>
          <a
            href={href}
            class:list={[
              'font-ui uppercase tracking-wide text-sm transition-colors duration-200',
              currentPath === href || (href !== '/' && currentPath.startsWith(href))
                ? 'text-burgundy'
                : 'text-warm-dark hover:text-burgundy',
            ]}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>

    <!-- Mobile hamburger (CSS-only using details/summary) -->
    <details class="md:hidden relative" id="mobile-nav">
      <summary class="list-none cursor-pointer p-3 -mr-3 min-w-[44px] min-h-[44px] flex items-center justify-center">
        <span class="sr-only">Menüü</span>
        <svg class="w-6 h-6 text-warm-dark" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </summary>
      <ul class="absolute right-0 top-full mt-2 bg-warm-white border border-black/10 rounded shadow-lg py-2 min-w-[180px]">
        {navLinks.map(({ label, href }) => (
          <li>
            <a
              href={href}
              class:list={[
                'block px-5 py-3 font-ui uppercase tracking-wide text-sm transition-colors duration-200 min-h-[44px] flex items-center',
                currentPath === href || (href !== '/' && currentPath.startsWith(href))
                  ? 'text-burgundy'
                  : 'text-warm-dark hover:text-burgundy hover:bg-black/5',
              ]}
            >
              {label}
            </a>
          </li>
        ))}
      </ul>
    </details>
  </nav>
</header>

<style>
  details summary::-webkit-details-marker {
    display: none;
  }
</style>

<script>
  // Close mobile nav when a link is clicked
  const nav = document.getElementById('mobile-nav');
  if (nav) {
    nav.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        (nav as HTMLDetailsElement).open = false;
      });
    });
  }
</script>
