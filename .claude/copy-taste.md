# Copy Taste Log

Accumulated picks + whys from `/copy-iterate` runs. Recent entries outweigh old ones when
they conflict. Each entry: date, surface, final copy, every round's pick + why (verbatim),
and prediction hits/misses.

---
